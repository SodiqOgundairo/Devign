{
  "name": "devign-figma-plugin",
  "version": "1.0.0",
  "description": "Figma plugin that imports the Devign design system — variables, styles, and auto-layout components",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "dev": "node build.js --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.93.0",
    "typescript": "^5.7.3",
    "esbuild": "^0.20.0",
    "concurrently": "^8.2.0"
  }
}